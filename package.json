{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "devDependencies": {
    "turbo": "^1.10.4"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node -e \"console.log('Current working directory:', process.cwd());\" && node apps/api/dist/main",
    "postinstall": "prisma generate"
  }
}
