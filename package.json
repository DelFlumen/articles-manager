{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "devDependencies": {
    "turbo": "^1.10.4"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node console.log(process.cwd()); node api/dist/main",
    "postinstall": "prisma generate"
  }
}
